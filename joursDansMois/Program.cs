// See https://aka.ms/new-console-template for more information

namespace Projet01
{
    class Program
    {
        static void Main()
        {
            /*
             
             float chiffre;
            Console.WriteLine("Entrez un nombre : ");
            chiffre = float.Parse(Console.ReadLine());
            Console.WriteLine("Le carré de " + chiffre + " est " + (chiffre * chiffre));
            
--------------------------------------------------------------------------
            decimal montant;
            Console.WriteLine("Quel est le prix de vente : ");
            montant = decimal.Parse(Console.ReadLine());
            Console.WriteLine("\n Montant   :   " + Math.Round(montant, 2) + " $" +
                              "\n TPS       :   " + Math.Round(montant* 0.05m,2) + " $" +
                              "\n TVQ       :   " + Math.Round(montant * 0.09975m,2) + " $" +
                              "\n Total     :   " + (Math.Round(montant,2) + Math.Round(montant * 0.09975m,2) + Math.Round(montant* 0.05m,2)) + " $") ;
            ----------------------------------------------------
            
            decimal temperC;
            
                Console.WriteLine("Entrez la température de l’eau en celsius : ");
                temperC = decimal.Parse(Console.ReadLine());
                
                if (temperC >= 0m && temperC <= 100m)
                {
                    Console.WriteLine("L’eau est dans un état liquide ");
                    

                }

                else if (temperC > 100m)
                    {
                        Console.WriteLine("L’eau est dans un état gazeux ");
                    }
                    
                    
                    
                
                else if(temperC < 0m)

                {
                    Console.WriteLine("L’eau est dans un état solide ");
                    
                }
               
                
                
                    
        */
            
                
            

        }
    }
}